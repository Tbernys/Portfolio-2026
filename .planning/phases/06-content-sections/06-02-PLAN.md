---
phase: 06-content-sections
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - index.html
autonomous: false
requirements:
  - CTCT-01
  - CTCT-03

must_haves:
  truths:
    - "The contact form submits to a real Formspree endpoint and shows a success message"
    - "The entire clients + contact section renders correctly in the browser with entrance animations"
  artifacts:
    - path: "index.html"
      provides: "Real Formspree FORM_ID in form action attribute"
      contains: "formspree.io/f/"
  key_links:
    - from: "#contact-form action attribute"
      to: "Formspree dashboard"
      via: "Real FORM_ID replacing placeholder"
      pattern: "formspree\\.io/f/[a-zA-Z0-9]"

user_setup:
  - service: formspree
    why: "Contact form backend — receives submissions and forwards to email"
    env_vars: []
    dashboard_config:
      - task: "Create free account at https://formspree.io"
        location: "formspree.io → Sign Up"
      - task: "Create a new form for contact@tombernys.com"
        location: "Formspree Dashboard → New Form → set recipient email"
      - task: "Copy the Form ID (the part after /f/ in the endpoint URL)"
        location: "Formspree Dashboard → Form → Integration → endpoint URL"
---

<objective>
Replace the Formspree placeholder FORM_ID with the real one from user's Formspree dashboard, then visually verify both content sections render and function correctly.

Purpose: Gates the contact form on a real backend endpoint and confirms visual quality before Phase 7.
Output: Working contact form submission, verified visual render of both sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-content-sections/06-CONTEXT.md
@.planning/phases/06-content-sections/06-01-SUMMARY.md
@index.html
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create Formspree account and provide FORM_ID</name>
  <files>index.html</files>
  <read_first>
    - index.html (search for REPLACE_WITH_FORM_ID to confirm placeholder is present)
  </read_first>
  <action>
User must create a Formspree form to receive contact submissions:

1. Go to https://formspree.io and create a free account (or log in)
2. Click "New Form" — set recipient email to contact@tombernys.com
3. Copy the Form ID from the endpoint URL: `https://formspree.io/f/{FORM_ID}`
4. Provide the FORM_ID string (e.g., "xpznqkba")

Once user provides the FORM_ID, the executor will replace `REPLACE_WITH_FORM_ID` in the form action attribute in index.html with the real ID.
  </action>
  <verify>
    <automated>cd "/Users/tom/Desktop/ PORTFOLIO 2026" && grep 'formspree.io/f/' index.html</automated>
  </verify>
  <resume-signal>Provide your Formspree FORM_ID (the alphanumeric string after /f/ in the endpoint URL), or type "skip" to keep the placeholder for now</resume-signal>
  <acceptance_criteria>
    - If user provides ID: index.html `form action` contains `formspree.io/f/` followed by a real alphanumeric ID (not REPLACE_WITH_FORM_ID)
    - If user skips: REPLACE_WITH_FORM_ID remains as-is (form will 404 on submit but all other functionality works)
  </acceptance_criteria>
  <done>Formspree FORM_ID is either inserted into index.html or explicitly deferred by user.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of clients and contact sections</name>
  <files>index.html</files>
  <read_first>
    - index.html (the #clients and #contact sections)
  </read_first>
  <action>
Visual verification of all Phase 6 content sections. The executor opens the page and presents verification steps to the user. No code changes — pure visual/functional confirmation.
  </action>
  <what-built>
    Plan 06-01 built the clients logo row (6 placeholders in a horizontal flex row with title "Ils m'ont fait confiance") and the contact form (3 fields, glassmorphism submit button, inline feedback, mailto link). Both sections use the existing .content-section entrance animation.
  </what-built>
  <how-to-verify>
    1. Open index.html in the browser (or reload if already open)
    2. Scroll down past the projects section to the clients section
    3. Verify: "Ils m'ont fait confiance" title appears, 6 placeholder logos visible in a horizontal row, accent color (#D4C5B2)
    4. Continue scrolling to the contact section
    5. Verify: "Contact" title, 3 form fields (Nom, Email, Message), glassmorphism "Envoyer" button
    6. Verify: "Ou directement : contact@tombernys.com" mailto link visible below the form
    7. Click the mailto link — should open mail client
    8. Try submitting the form (if Formspree FORM_ID was provided): fill all 3 fields, click Envoyer, confirm success message appears in green
    9. Verify both sections fade in with the entrance animation when first scrolled into view
  </how-to-verify>
  <verify>User visual approval</verify>
  <resume-signal>Type "approved" if both sections look and work correctly, or describe any issues</resume-signal>
  <acceptance_criteria>
    - User confirms logos section renders correctly with horizontal row layout
    - User confirms contact form is visually consistent with the dark/glassmorphism aesthetic
    - User confirms entrance animations fire on scroll
    - User confirms mailto link works
    - If Formspree ID provided: user confirms form submission shows success feedback
  </acceptance_criteria>
  <done>Both content sections are visually verified by the user and match the design intent from CONTEXT.md decisions.</done>
</task>

</tasks>

<verification>
1. `grep 'REPLACE_WITH_FORM_ID' index.html` returns 0 matches (if user provided real ID) or 1 match (if skipped)
2. User visual approval of both sections
</verification>

<success_criteria>
- Formspree FORM_ID either inserted or explicitly deferred
- User has visually verified both sections render correctly
- Entrance animations confirmed working
- Contact form submission confirmed working (if FORM_ID provided)
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-sections/06-02-SUMMARY.md`
</output>
