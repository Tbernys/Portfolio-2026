---
phase: 01-scaffolding
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - fonts/comfortaa_bold.typeface.json
autonomous: false
requirements:
  - TECH-03

must_haves:
  truths:
    - "The font file fonts/comfortaa_bold.typeface.json exists and contains valid glyph data"
    - "The site deploys to a static host and loads without module import errors on the production URL"
    - "Three.js test cube renders on the deployed production URL"
    - "OG meta tags render a link preview when the deployed URL is pasted into an OG validator"
  artifacts:
    - path: "fonts/comfortaa_bold.typeface.json"
      provides: "Converted Comfortaa Bold font in Three.js typeface format for TextGeometry in Phase 3"
      contains: "glyphs"
  key_links:
    - from: "fonts/comfortaa_bold.typeface.json"
      to: "Three.js FontLoader (Phase 3)"
      via: "FontLoader.load('./fonts/comfortaa_bold.typeface.json')"
      pattern: "comfortaa.*typeface"

user_setup:
  - service: facetype.js
    why: "Convert Comfortaa Bold TTF to typeface.json for Three.js TextGeometry"
    dashboard_config:
      - task: "Download Comfortaa Bold TTF from Google Fonts, upload to facetype.js, download typeface.json"
        location: "https://fonts.google.com/specimen/Comfortaa -> Download family; then https://gero3.github.io/facetype.js/"
  - service: static-host
    why: "Deploy to validate import map works on production HTTPS"
    dashboard_config:
      - task: "Deploy project root to any static host (Vercel, Netlify, or GitHub Pages)"
        location: "Vercel CLI: npx vercel --yes; or Netlify CLI: npx netlify deploy --prod --dir ."
---

<objective>
Convert the display font for Phase 3 readiness and deploy the scaffold to a static host to prove the CDN import map works in production.

Purpose: The font conversion is a Phase 3 blocker (STATE.md). Deployment validation proves TECH-03 and confirms the import map resolves on a real HTTPS host -- the single most important validation of Phase 1, since import maps fail silently under file:// but work correctly over HTTP/HTTPS.
Output: fonts/comfortaa_bold.typeface.json (converted font), verified production deployment
</objective>

<execution_context>
@/Users/tom/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding/01-CONTEXT.md
@.planning/phases/01-scaffolding/01-RESEARCH.md
@.planning/phases/01-scaffolding/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Convert Comfortaa Bold to typeface.json</name>
  <files>fonts/comfortaa_bold.typeface.json</files>
  <action>
The user must convert the display font using a browser-based tool. Claude cannot perform this step because facetype.js runs entirely in the browser with file upload UI.

**Steps for user:**
1. Go to https://fonts.google.com/specimen/Comfortaa
2. Click "Download family" button -> extract the ZIP
3. Find `Comfortaa-Bold.ttf` in the extracted files
4. Go to https://gero3.github.io/facetype.js/
5. Upload `Comfortaa-Bold.ttf`
6. **Important:** Check "Restrict character set" and select only Latin Basic (reduces file from ~1MB to ~50KB)
7. Click convert/generate
8. Download the resulting JSON file
9. Save it as `fonts/comfortaa_bold.typeface.json` in the project directory (create the `fonts/` directory if needed)
  </action>
  <verify>Confirm fonts/comfortaa_bold.typeface.json exists in the project directory after user completes the manual conversion.</verify>
  <done>fonts/comfortaa_bold.typeface.json file exists in the project directory.</done>
</task>

<task type="auto">
  <name>Task 2: Validate font file and deploy to static host</name>
  <files>fonts/comfortaa_bold.typeface.json</files>
  <action>
**Font validation:**
- Read `fonts/comfortaa_bold.typeface.json` and confirm it is valid JSON
- Confirm it contains a `glyphs` key with glyph data
- Confirm it contains entries for at least the basic Latin characters (A-Z, a-z, 0-9)
- Log the file size -- should be roughly 30-150KB for Latin Basic subset

**Deployment:**
- Attempt deployment using Vercel CLI: `npx vercel --yes` from the project root
- If Vercel CLI is not available or fails, try Netlify: `npx netlify deploy --prod --dir .`
- If neither CLI works, create a checkpoint for the user to deploy manually
- Record the deployed URL

**Post-deployment validation (run from CLI):**
- `curl -s -o /dev/null -w "%{http_code}" {DEPLOYED_URL}` -- should return 200
- `curl -s {DEPLOYED_URL} | grep -c "importmap"` -- should return 1 (import map present)
- `curl -s {DEPLOYED_URL} | grep -c "three@0.175.0"` -- should return at least 2 (both import map entries)
- `curl -s -o /dev/null -w "%{http_code}" {DEPLOYED_URL}/og-image.jpg` -- should return 200

If the CLI deploy fails due to auth, pause and ask the user to authenticate, then retry.
  </action>
  <verify>
1. `cat fonts/comfortaa_bold.typeface.json | python3 -c "import sys,json; d=json.load(sys.stdin); print('glyphs' in d, len(d.get('glyphs',{})))"` returns True with glyph count > 50
2. Deployed URL returns HTTP 200
3. Deployed URL source contains the import map with three@0.175.0
  </verify>
  <done>Font file is valid typeface.json with Latin glyph data. Site is deployed on a static host, returns 200, and the import map is present in the served HTML.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify production deployment in browser</name>
  <files>index.html</files>
  <action>
Claude automated the deployment in Task 2. Now the user verifies the production site works correctly in a real browser.

**What was built:**
Complete Phase 1 scaffold deployed to production: index.html with Three.js import map, semantic HTML, meta tags, favicon, and OG image. Font file converted and ready for Phase 3.

**How to verify:**
1. Open the deployed URL in your browser
2. Confirm the rotating cube appears on a black background
3. Open browser DevTools console -- should show "[Phase 1] Three.js r175 loaded via import map" and ZERO errors
4. Check the browser tab: TB favicon visible, title shows "Tom Bernys | Portfolio"
5. Verify "Tom Bernys" heading and "Monteur Video & Motion Designer" subtitle are visible on the page
6. Paste the deployed URL into https://www.opengraph.xyz/ -- confirm the link preview shows title, description, and placeholder image
7. (Optional) Test on your phone -- page should load without errors
  </action>
  <verify>User confirms all 6 verification steps pass in the browser.</verify>
  <done>User has approved the production deployment -- rotating cube renders, no console errors, meta tags validate, hero text is visible.</done>
</task>

</tasks>

<verification>
1. fonts/comfortaa_bold.typeface.json exists and is valid JSON with glyphs
2. Production URL returns 200 and serves the import map correctly
3. Three.js loads and renders the test cube on production (no console errors)
4. OG validator shows correct link preview for the deployed URL
5. Semantic HTML is visible in view-source on the production URL
</verification>

<success_criteria>
- Font file ready for Phase 3 TextGeometry (TECH-03 prep / Phase 3 blocker resolved)
- Site deploys on a static host with zero configuration (TECH-03)
- Import map resolves Three.js correctly on production HTTPS
- OG link preview renders when URL is shared
- No console errors on the production deployment
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding/01-02-SUMMARY.md`
</output>
